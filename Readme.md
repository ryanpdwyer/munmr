# Mount Union NMR programs

## Notes

- Lots of issues trying to run cascade tensorflow model in the main streamlit process - as a result, I switched to just running it in a separate process via `run_cascade.py`.
